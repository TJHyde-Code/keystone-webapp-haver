@model HaverGroupProject.Models.Vendor

@{
    ViewData["Title"] = "Edit Vendor";
}

<h1>Edit</h1>

<div class="card shadow-lg p-4 mb-5 bg-body-tertiary rounded">
    <h4>Vendor</h4>
    <hr />
    <div class="row">
        <div class="col-md-4">
            <form asp-action="Edit">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <input type="hidden" asp-for="ID" />
                <div class="form-group">
                    <label asp-for="VendorName" class="control-label fw-bold"></label>
                    <input asp-for="VendorName" class="form-control input-width" />
                    <span asp-validation-for="VendorName" class="text-danger"></span>
                </div>
                <div class="d-flex gap-3 mt-4">
                    <div class="form-group">
                        <button type="submit" class="btn btn-primary px-4 py-2">
                            <i class="bi bi-save-fill me-2"></i>Save
                        </button>
                    </div>
                    <div>
                        <a asp-action="Index" class="btn btn-success px-4 py-2">
                            <i class="bi bi-arrow-left me-2"></i>Back to List
                        </a>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>


@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
