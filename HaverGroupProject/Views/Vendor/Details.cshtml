@model HaverGroupProject.ViewModels.VendorDetailsViewModel

@{
    ViewData["Title"] = "Vendor Details";
}

<div class="container mt-4">
    <div class="card shadow-lg p-4 mb-4 rounded-3">
        <div class="card-header bg-light py-3">
            <h1 class="display-6 mb-0">
                <i class="bi bi-building me-2"></i>@Html.DisplayFor(model => model.Vendors.VendorName)
            </h1>
        </div>

        <div class="card-body">
            <div class="row mb-4">
                <div class="col-md-8">
                    <div class="row g-3">
                        <div class="col-md-6">
                            <div class="border-bottom pb-2">
                                <h5 class="text-muted mb-3"><i class="bi bi-person-badge me-2"></i>Contact Information</h5>
                                <dl class="row">
                                    <dt class="col-sm-4">Contact Name</dt>
                                    <dd class="col-sm-8">@Html.DisplayFor(model => model.Vendors.VendorContactName)</dd>

                                    <dt class="col-sm-4">Email</dt>
                                    <dd class="col-sm-8">
                                        <a href="mailto:@Model.Vendors.VendorEmail" class="text-decoration-none">
                                            @Html.DisplayFor(model => model.Vendors.VendorEmail)
                                        </a>
                                    </dd>

                                    <dt class="col-sm-4">Phone</dt>
                                    <dd class="col-sm-8">@Html.DisplayFor(model => model.Vendors.VendorPhone)</dd>
                                </dl>
                            </div>
                        </div>

                        <div class="col-md-6">
                            <div class="border-bottom pb-2">
                                <h5 class="text-muted mb-3"><i class="bi bi-geo-alt me-2"></i>Address</h5>
                                <p class="mb-0">@Html.DisplayFor(model => model.Vendors.VendorAddress)</p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Active Orders Section -->
            <div class="mt-4">
                <div class="alert @(Model.ActiveOrdersCount > 0 ? "alert-info" : "alert-warning") d-flex align-items-center">
                    <i class="bi @(Model.ActiveOrdersCount > 0 ? "bi-list-check" : "bi-exclamation-triangle") me-2 fs-4"></i>
                    <div>
                        <strong>Active Orders:</strong> @Model.ActiveOrdersCount
                    </div>
                </div>

                @if (Model.ActiveOrdersCount > 0)
                {
                    <div class="card mt-3">
                        <div class="card-header bg-light">
                            <h5 class="mb-0"><i class="bi bi-card-checklist me-2"></i>Active Orders</h5>
                        </div>
                        <div class="card-body p-0">
                            <ul class="list-group list-group-flush">
                                @foreach (var order in Model.ActiveOrders)
                                {
                                    <li class="list-group-item d-flex justify-content-between align-items-center">
                                        <div>
                                            <strong>SO# @order.SalesOrdNum</strong>
                                            <div class="text-muted small">
                                                Kickoff: @order.KickoffMeeting.Value.ToString("MMM dd, yyyy")
                                            </div>
                                        </div>
                                        <span class="badge bg-primary rounded-pill">Active</span>
                                    </li>
                                }
                            </ul>
                        </div>
                    </div>
                }
                else
                {
                    <div class="text-center py-3 text-muted">
                        <i class="bi bi-inbox fs-1"></i>
                        <p class="fs-5 mt-2">No active orders for this vendor</p>
                    </div>
                }
            </div>
        </div>
    </div>

    <div class="d-flex gap-2 mt-3">
        <a asp-action="Edit" asp-route-id="@Model.Vendors?.ID" class="btn btn-primary px-4">
            <i class="bi bi-pencil-square me-2"></i>Edit
        </a>
        <a asp-action="Index" class="btn btn-outline-secondary px-4">
            <i class="bi bi-arrow-left me-2"></i>Back to List
        </a>
    </div>
</div>

<style>
    .card {
        border: none;
        transition: transform 0.2s;
    }
    .card:hover {
        transform: translateY(-2px);
    }
    dt {
        font-weight: 500;
        color: #6c757d;
    }
    dd {
        font-weight: 400;
    }
    .list-group-item {
        transition: background-color 0.2s;
    }
    .list-group-item:hover {
        background-color: #f8f9fa;
    }
</style>